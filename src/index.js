import ReactDOM from 'react-dom';
import App from './App';
import Spinner from "./Spinner"

ReactDOM.render(
  <div id="wrapper">
      <Spinner></Spinner>
  </div>
  ,
  document.getElementById('root')
);
